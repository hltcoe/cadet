servlets {
    retrieve {
        host = "localhost"
        port = 44333
        provider = "edu.BlahRetrieverProvider"
    }
    search {
        host = "localhost"
        port = 8088
        provider = "edu.jhu.hlt.concrete.search.MockSearchProvider"
    }
    feedback {
        dump_dir = "/tmp/"
    }
}